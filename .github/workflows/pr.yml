name: PR

on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Pin: #12858 merge — docker context integrity gate + traceability context fix.
    uses: paperclipai/paperclip/.github/workflows/pr-trusted.yml@856813ba3a083f23694b8554104b3e50abcb1363
